# Install torch separately with the CUDA build matching your GPU/driver, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# then install the rest:
#   pip install -r requirements.txt
torch>=2.6
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
